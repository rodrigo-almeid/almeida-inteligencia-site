server {
    listen 80;
    server_name _;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name _;

    ssl_certificate /etc/ssl/cloudflare/cert.pem;
    ssl_certificate_key /etc/ssl/cloudflare/key.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    resolver 127.0.0.11 valid=10s;

    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/css application/javascript image/svg+xml;

    # Landing page
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Dashboard pós-login
    location /dashboard/ {
        alias /usr/share/nginx/html/dashboard/;
        try_files $uri $uri/ /dashboard/index.html;
    }

    # Painel admin
    location /admin/ {
        alias /usr/share/nginx/html/admin/;
        try_files $uri $uri/ /admin/index.html;
    }

    # API backend (Almeida portal)
    location /api/ {
        set $upstream_backend http://backend:8001;
        proxy_pass $upstream_backend/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Módulos Centrais (credenciais + financeiro)
    location /central/ {
        set $upstream_central http://central-backend:8000;
        proxy_pass $upstream_central/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }

    location /credenciais/ {
        set $upstream_central http://central-backend:8000;
        proxy_pass $upstream_central/credenciais/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /financeiro/ {
        set $upstream_central http://central-backend:8000;
        proxy_pass $upstream_central/financeiro/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Assistente Virtual (frontend)
    location /assistente-painel/ {
        set $upstream_central http://central-backend:8000;
        proxy_pass $upstream_central/assistente-painel/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Assistente Virtual (webhook Meta + API config)
    location /assistente/ {
        set $upstream_central http://central-backend:8000;
        proxy_pass $upstream_central/assistente/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }

    # Agendamento Inteligente (frontend)
    location /agendamento-painel/ {
        set $upstream_central http://central-backend:8000;
        proxy_pass $upstream_central/agendamento-painel/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Agendamento Inteligente (API + webhook)
    location /agendamento/ {
        set $upstream_central http://central-backend:8000;
        proxy_pass $upstream_central/agendamento/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }

    # Classificador de E-mails (frontend + API servidos pelo email-backend)
    location /email/ {
        set $upstream_email http://email-backend:8000;
        proxy_pass $upstream_email/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }

    # Cache de estáticos (apenas arquivos locais do nginx, não proxied)
    location ~* ^/(dashboard|admin)/.*\.(css|js|svg|ico|png|jpg|woff2?)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
}
